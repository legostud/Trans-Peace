/* Initialize {{pascalCase name}} component */

const init{{pascalCase name}} = () => {
  const {{camelCase name}}s = [...document.querySelectorAll(".js-{{kebabCase name}}")];
  
  {{camelCase name}}s.forEach((element) => {
    /* Component logic */
  });
}

export default init{{pascalCase name}};
