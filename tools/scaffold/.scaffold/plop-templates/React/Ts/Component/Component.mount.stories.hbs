import {FC} from "react";
import { mockData } from "@/js/components/{{pascalCase name}}";

const Template: FC = (args) => (
  <div className="js-{{kebabCase name}}-mount" data-mock={JSON.stringify(args)} />
);

export default {
  title: "Components/{{pascalCase name}}",
};

export const {{camelCase name}} = Template.bind({});
{{camelCase name}}.args = mockData;
